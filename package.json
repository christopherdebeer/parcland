{
  "name": "parcland",
  "version": "1.0.0",
  "description": "Interactive canvas-based diagramming application",
  "scripts": {
    "test": "jest",
    "build": "esbuild next/*.js --bundle --minify --outdir=dist",
    "lint": "eslint next/",
    "format": "prettier --write next/"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.23.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.5.3"
  }
}
