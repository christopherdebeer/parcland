{
  "version": 2,
  "buildCommand": "npm install && npm run build:all",
  "outputDirectory": "next-api/.next",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
