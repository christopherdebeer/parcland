{
  "version": 2,
  "buildCommand": "cd next-api && npm install && npm run build",
  "outputDirectory": "next-api/.next",
  "installCommand": "cd next-api && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
